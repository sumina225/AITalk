server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwt_db?useSSL=false&serverTimezone=UTC
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.example.jwtlogin.model

jwt:
  secret: your_jwt_secret_key
  access-token-expiration: 900000   # 15분 (밀리초 단위)
  refresh-token-expiration: 604800000 # 7일 (밀리초 단위)
